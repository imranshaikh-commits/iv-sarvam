# Secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Client data (NDA-protected)
data/raw/
data/processed/
data/tagging/*.csv
!data/tagging/tagging_template.csv

# Runtime traces and logs
traces/
*.log

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Node / Frontend
node_modules/
.next/
.nuxt/
.cache/
dist/
.wrangler/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Hermes runtime data (large, local)
backend/hermes/hermes-data/
backend/hermes/logs/

# Generated proposals (also client-sensitive)
generated/

# Eval fixtures contain CLIENT-CONFIDENTIAL proposal content (client names,
# scope, sizing, commercial structure). This repository is public — fixtures
# must never be committed. Keep them locally or in the private Drive alongside
# the source proposals. Only docs/evals/README.md (the convention) is tracked.
docs/evals/*
!docs/evals/README.md

# Secrets — host-only env file (public repo)
sarvam.env
**/sarvam.env
*.env

# ingestion run artefacts
scripts/out/
scripts/out_partner/
scripts/out_partner_images/

# retrieval scorecards (contain client names)
retrieval_*.json
scripts/retrieval_*.json

# Asset review artefacts — embed client proposal imagery and client names
asset_review.html
ids.txt
scripts/asset_review.html
scripts/ids.txt
partner_review.html
partner_ids.txt
scripts/partner_review.html
scripts/partner_ids.txt
# Supabase table backups (client data)
sarvam-backups/
backups/

corpus_manifest*.csv
corpus_review_summary*.md

corpus_manifest*.csv
corpus_review_summary*.md

corpus_manifest*.csv
corpus_review_summary*.md

# generated proposals — real client content
deploy/*.docx
deploy/*.pdf
*.docx
*.pdf
# Image kit files (uploaded to storage, never committed)
asset_kit/
asset_kit.tar.gz
