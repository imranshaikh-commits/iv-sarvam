# Sarvam — Backlog / Deferred Items

Items intentionally deferred to a later phase. Not blockers.

- [ ] **Re-apply Sarvam + IV branding in Open WebUI** (deferred to final polish phase). During Sprint 3 the container was migrated from a hand-run `docker run` to Docker Compose management. User data (admin login, chat sessions) survived via the reused `open-webui` volume, but custom branding (app name "Sarvam", IV logo/colors) was reset to Open WebUI defaults. Re-configure via Admin Panel > Settings > Interface, or bake into compose env (WEBUI_NAME etc.) when we do the final UX pass. Logged 2026-07-10.
- [ ] **AWS Budget alert ($10/month)** (deferred Sprint 3). Console-only task (connector can't set Budgets). Set via AWS Billing > Budgets > monthly cost budget, $10, email imran.shaikh@inspiritvision.com. Interim guardrails in place: 2GB swap, health-check script, unattended security updates. Logged 2026-07-10.
- [ ] **Sprint 4 Phase 1 DONE** (2026-07-13): sarvam-brain FastAPI service deployed to EC2 via docker compose, OpenAI-compatible endpoint, grounded retrieval over 11 proposals (1,413 chunks). Wired into Open WebUI as model 'sarvam-architect'. Verified: grounded answers with [n] citations, similarity 0.59-0.65 after retrieval-quality fixes (vendor-aware preference, fuzzy dedup, boilerplate exclusion). Bugs fixed en route: PostgREST 300 Multiple Choices (duplicate function overload + vector-param ambiguity → single text-param RPC), boilerplate crowding out specific content.
- [ ] **Sprint 4 Phase 2 NEXT**: compliance-matrix capability via Instructor (paste RFP → structured requirement extraction → coverage check covered/partial/missing/needs-human). Highest-value capability per design review.
- [ ] **Sprint 4 Phase 3**: Exa + Firecrawl research tools + verify-stage fact-checker (extract claims → re-search → flag mismatches). Needs EXA_API_KEY + FIRECRAWL_API_KEY in vault.
- [ ] **Retrieval quality - next levers if needed**: hybrid search (BM25 keyword + pgvector), re-ranking cross-encoder, metadata client/region detection, exclude more boilerplate section types.
