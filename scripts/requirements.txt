# ingest_v2.py — Sarvam proposal ingestion
# Text/PDF/image extraction
python-docx>=1.1.0
pypdf>=4.0.0
pillow>=10.0.0

# HTTP + progress
requests>=2.31.0
tqdm>=4.66.0

# Tesseract Python wrapper (system tesseract binary also required — apt install tesseract-ocr)
pytesseract>=0.3.10
